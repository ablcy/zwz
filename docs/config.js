/* ==========================================================================
   声刻 VoxScript · 前端部署配置（可选，可被用户在页面上的设置覆盖）

   VOXSCRIPT_API_BASE
     后端 API 的基地址。空字符串 "" = 跟随当前站点（前后端同源部署，默认）。
     把本页放在 GitHub Pages 等纯静态托管上时，填你自建后端的公网地址，例如：
       window.VOXSCRIPT_API_BASE = "https://voxscript-api.example.com";

   优先级：URL 参数 ?api=https://xxx（最高，会写入 localStorage）
          > 页面上的「后端服务」输入框（保存在浏览器 localStorage）
          > 本文件（全站默认值）
          > 同源
   其中 URL 参数与输入框留空都表示"回到同源"。

   注意：本文件会被浏览器公开下载，只放后端地址，不要写任何密钥 / Token。
   ========================================================================== */
window.VOXSCRIPT_API_BASE = "";
